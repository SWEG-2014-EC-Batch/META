//printPattern5x.cpp
#include <iostream>
using namespace std;
int
main ()
{
  int  n = 1, m = 0, g,f=7,h=8;

  for (int size = 1; size <= f; size++)
    {
      for (int size = 1; size <= 1; size++)
  {
    cout << n;
  } g = n + 1;
      for (int size = 1; size <= m; size++)
  {
    cout <<" "<<g++<<"";
  } m++;
      cout << endl;
     
    }
     for(int size=1;size<=8;size++)
     {
         cout<<h--<<" ";
        
     }cout<<endl;
  
    cout<<"*****This is the last line*****";

  return 0;
}